import { Meta } from "@storybook/addon-docs/blocks";
import { Page, Rule, Rules, Section } from "./guide/Guide";
import { Surfaces as SurfaceGrid } from "./guide/Specimens";

<Meta title="Foundations/Surfaces" />

<Page
  eyebrow="Where it shows up"
  title="The same three rules, on every surface."
  lede="A warm surface. A navy voice. A single gold moment. The signature below is the template: name, role, a hairline, the company, the site. It is filled with a placeholder, not a person."
>
  <SurfaceGrid />
  <Section eyebrow="Signature" title="This is not where we improvise.">
    <Rules>
      <Rule>The name is Sansation. The role, the company, and the site are IBM Plex Mono.</Rule>
      <Rule>Ink is navy. The site address is Gold 700, small enough to clear the page.</Rule>
      <Rule>Copy the template. Do not add a second colour, a slogan, or a photograph.</Rule>
    </Rules>
  </Section>
</Page>
